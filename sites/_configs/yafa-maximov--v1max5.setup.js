for (const item of document.querySelectorAll('div.elementor-toggle-item')) { if (item.querySelector('.__ai-externalJobId')) continue; const title = (item.querySelector('a.elementor-toggle-title') || {}).textContent || ''; const hash = Array.from(title).reduce((h,c) => Math.imul(31,h) + c.charCodeAt(0) | 0, 0); const span = document.createElement('span'); span.className = '__ai-externalJobId'; span.style.display = 'none'; span.textContent = 'h-' + Math.abs(hash).toString(16); item.appendChild(span); }
